<?php

/*

    # Composer

        composer require server (loads server setup so we can use ./bin/console ...)
        composer require annotations (loads annotations feature)


    # Console cmds

        ./bin/console --version (shows version)
        ./bin/console server:run (starts dev server)
        ./bin/console (shows all options available)

        





*/



?>